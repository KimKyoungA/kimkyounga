using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerIDLE : PlayerFSMState
{

	// Use this for initialization(초기화)
	void Start () {
		
	}
	
	// Update is called once per frame(프레임마다 반복)
	void Update () {
        Debug.Log("IDLE");
	}
}
